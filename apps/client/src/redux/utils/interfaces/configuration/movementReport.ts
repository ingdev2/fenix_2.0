interface MovementReport {
  id: number;
  mov_r_name: string;
  mov_r_description: string;
  mov_r_time: number;
  mov_r_status: boolean;
  createdAt: string;
  updateAt: string;
  deletedAt: string | null;
}

